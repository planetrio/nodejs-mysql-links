<div class="app-content content">
    <div class="content-wrapper">

        <div class="content-header row">
            <div class="content-header-left col-md-8 col-12 mb-2 breadcrumb-new">
                <h3 class="content-header-title mb-0 d-inline-block"><i class="las la-network-wired"></i> Proxy </h3>
                <div class="row breadcrumbs-top d-inline-block">
                    <div class="breadcrumb-wrapper col-12">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
                            <li class="breadcrumb-item active">Proxy Instances</li>
                        </ol>
                    </div>
                </div>
            </div>
            <div class="content-header-right col-md-4 col-12 d-none d-md-inline-block">
                <div class="btn-group float-md-right">
                    <a class="btn-gradient-secondary btn-sm white" href="/proxy/add">+ Create Proxy</a>
                </div>
            </div>
        </div>

        <div class="content-body">
            <div class="row">
                <div class="col-12 col-md-9">

                    {{#if rows.[0]}}
                    <section class="card">
                        <div class="card-content">
                            <div class="table-responsive">
                                <table id="recent-orders" class="table table-hover table-xl mb-0">
                                    <thead>
                                        <tr>
                                            <th class="border-top-0">Instance</th>
                                            <th class="border-top-0">Status</th>
                                            <th class="border-top-0">Host:Port</th>
                                            <th class="border-top-0">Last Update</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {{#each rows}}
                                        <tr>
                                            <td class="text-truncate"><a href="/proxy/view/{{InstanceId}}">{{Name}}<div class="badge-sm text-muted mb-0">{{InstanceId}}</div></a></td>
                                            <td class="text-truncate"><i class="las la-check-circle success font-medium-1"></i>{{Status}}</td>
                                            <td class="text-truncate">us1.planetr.io:7788</td>
                                            <td class="text-truncate">{{UpdatedAt}}</td>
                                        </tr>
                                        {{/each}}
                                    </tbody>
                                </table>
                            </div>

                        </div>
                    </section>
                    {{else}}
                    No Proxy running yet.
                    {{/if}}

                </div>
                <div class="col-12 col-md-3">
                    <h6 class="mt-2">What is Edge Proxy?</h6>
                    <p>Planter's decentralised network proxy (SOCKS5) allows you to benefit from traffic exiting from edge nodes located all over the world. </p>
                    <p>This includes both consumer IP addresses and data centre IP addresses.</p>
                </div>
            </div>
            
        </div>
    </div>
</div>
